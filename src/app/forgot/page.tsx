import ResetPassWord from "@/component/reset-password";
import Wrapper from "@/layouts/Wrapper";

export const metadata = {
  title: "Reset PassWord Bico - ICO & Crypto Landing React Next js Template",
};
const index = () => {
  return (
    <Wrapper>
      <ResetPassWord />
    </Wrapper>
  );
};

export default index;
